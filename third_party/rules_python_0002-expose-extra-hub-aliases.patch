diff --git a/python/private/pypi/multi_pip_parse.bzl b/python/private/pypi/multi_pip_parse.bzl
index 60496c2e..c57c2b55 100644
--- a/python/private/pypi/multi_pip_parse.bzl
+++ b/python/private/pypi/multi_pip_parse.bzl
@@ -56,6 +56,8 @@ _process_requirements(
     # we would be creating directories for each.
     macro_tmpl = "@%s_{}//:{}" % rctx.attr.name
 
+    extra_aliases_map = render.indent(render.dict(rctx.attr.extra_hub_aliases)).lstrip()
+
     requirements_bzl = """\
 # Generated by python/pip.bzl
 
@@ -64,6 +66,7 @@ load("@{rules_python}//python:pip.bzl", "whl_library_alias", "pip_utils")
 
 _wheel_names = []
 _version_map = dict()
+_extra_aliases_map = {extra_aliases_map}
 def _process_requirements(pkg_labels, python_version, repo_prefix):
     for pkg_label in pkg_labels:
         wheel_name = Label(pkg_label).package
@@ -91,6 +94,9 @@ def data_requirement(name):
 def dist_info_requirement(name):
     return "{macro_tmpl}".format(pip_utils.normalize_name(name), "dist_info")
 
+def extra_requirement(name, target):
+    return "{macro_tmpl}".format(pip_utils.normalize_name(name), target)
+
 def install_deps(**whl_library_kwargs):
 {install_deps_calls}
     for wheel_name in _wheel_names:
@@ -100,6 +106,7 @@ def install_deps(**whl_library_kwargs):
             default_version = "{default_version}",
             minor_mapping = {minor_mapping},
             version_map = _version_map[wheel_name],
+            extra_aliases = _extra_aliases_map.get(wheel_name, []),
         )
 """.format(
         name = rctx.attr.name,
@@ -110,6 +117,7 @@ def install_deps(**whl_library_kwargs):
         rules_python = rules_python,
         default_version = rctx.attr.default_version,
         minor_mapping = render.indent(render.dict(rctx.attr.minor_mapping)).lstrip(),
+        extra_aliases_map = extra_aliases_map
     )
     rctx.file("requirements.bzl", requirements_bzl)
     rctx.file("BUILD.bazel", "exports_files(['requirements.bzl'])")
@@ -120,6 +128,7 @@ _multi_pip_parse = repository_rule(
         "default_version": attr.string(),
         "minor_mapping": attr.string_dict(),
         "pip_parses": attr.string_dict(),
+        "extra_hub_aliases": attr.string_list_dict(default={}),
         "_rules_python_workspace": attr.label(default = Label("//:WORKSPACE")),
     },
 )
@@ -163,4 +172,5 @@ def multi_pip_parse(name, default_version, python_versions, python_interpreter_t
         default_version = default_version,
         pip_parses = pip_parses,
         minor_mapping = minor_mapping,
+        extra_hub_aliases = kwargs.get("extra_hub_aliases", {})
     )
diff --git a/python/private/pypi/whl_library_alias.bzl b/python/private/pypi/whl_library_alias.bzl
index 66c3504d..d533889c 100644
--- a/python/private/pypi/whl_library_alias.bzl
+++ b/python/private/pypi/whl_library_alias.bzl
@@ -25,7 +25,9 @@ def _whl_library_alias_impl(rctx):
         default_repo_prefix = None
     version_map = rctx.attr.version_map.items()
     build_content = ["# Generated by python/pip.bzl"]
-    for alias_name in ["pkg", "whl", "data", "dist_info"]:
+    standard_aliases = ["pkg", "whl", "data", "dist_info"]
+    all_aliases = standard_aliases + (rctx.attr.extra_aliases or [])
+    for alias_name in all_aliases:
         build_content.append(_whl_library_render_alias_target(
             alias_name = alias_name,
             default_repo_prefix = default_repo_prefix,
@@ -98,6 +100,7 @@ whl_library_alias = repository_rule(
         "minor_mapping": attr.string_dict(mandatory = True),
         "version_map": attr.string_dict(mandatory = True),
         "wheel_name": attr.string(mandatory = True),
+        "extra_aliases": attr.string_list(default=[]),
         "_rules_python_workspace": attr.label(default = Label("//:WORKSPACE")),
     },
 )
